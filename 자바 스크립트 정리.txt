자바 스크립트

1. 문자는 항상 " " or' '로 감싸주기 뒤에 항상 ; 붙이기(필수는 아님)                           

2. alert = 경고문 , console.log = 로그 찍는 것

3. 만약 변수가 겹치게 된다면 처음적용한 변수는 덮어 씌어지기 때문에 let 과 const   변수의 생성과정: 1. 선언단계 2. 초기화 단계 3. 할당 단계              var는 선언 및 초기화 단계를 한번에 한다.   var는한번 선언된 문장을 선언할 수 있다. var는 hoisting이기 때문에 항상 최상위에 위치한다.	
 	let은 3단계를 거침		const는 한번에 한다.						hoisting = 스코프 내부 어디서든 변수 선언은 최상위에 선언된 것 처럼 행동
let 과 const는 호이스팅 존에 영향을 받는다. 둘다 블록 스코프라 { } 안에 변수를 선언 했다면 그 변수는 밖에서 사용 불가능   함수안에 선언된 변수는 밖에서 못쓴다.		
let name = "mike" 
name = "google"    이런식이면 됨(의도적 let 생략)

const = 절대적으로 바뀌지 않는 상수(대문자로 표현 상수라는것을 표현하기위해)

변수선언할때 주의점: _와 $,숫자만 표현가능, 첫글자는 숫자가 될 수 없다. 
` `로    alert(` 안녕하세요 ${name} 님.`)  (name은 앞에서 정의) 표현 가능

4. 사칙연산 가능 문자형도 더하기 가능 숫자형과 문자형이 더하기를 하면 숫자형이 문자형으로 변환된다.

5. null(객체 아님) = 있지만 비어있음 undefined = 선언되지 않음, 존재하지않음

6. typeof = 변수의 성질 알려줌 str int float 이런것

7. alert = 알려줌(일방적)  prompt  = 입력 받음 comfirm = 확인 받음 
prompt("여기에 입력하세요" , "뒤에는 예시임")      comfirm은 확인과 취소가 있음 
단점: 스크립트 일시 정지, 스타일링 불가능 

8. String() , Number() , Boolean() 순서대로 문자형 숫자형 불린형으로 변환 (명시적 형변환)
	Number(true),Number(False)는 각각 1,0으로 표현된다.
	Boolean(0,빈 문자열"",null,undefined,NaN)은 False로 출력된다. 나머지는 다 true
	Number(null)//0  Number(undefined)//0   Number('') // false 공백있으면 true

9. 자동 형변환: 나누기는 숫자형이 아니더라도 자동 변환이 된다.  
let num = 10;
let result = num++; // 증가 시키기 전의 값을 result에 놓습니다.
             = ++num; // 증가 시킨 값을 result에 놓습니다.

10. '===' // 일치연산자 즉 성질까지 비교해준다.

11. && = and (둘다 참)--- 첫번째 false 찾으면 멈춤  
  || = or (둘 중 하나만 되고 o)--->  true 하나만 찾아도 멈춤 
! = not 

12. for(초기값; 조건; 코드 실행 후 작업)	tmi: 보통 반복문 같은 것은 정확한 범위가 정해져 있으면 for 아니면 while 문 쓴다.
 
13.
switch = 케이스가 다양할떄 쓴다 if else 문과 유사하다.

14. 함수 내부에 존재하는 것을 지역변수 즉 안에서 밖에 사용할 수 없다. 전역 변수는 모든 곳에서 사용가능. 전역변수가 많아지면 복잡해 지므로 지역변수 설정을 꾸준히 하시길 
    함수는 한번에 한작업하기, 읽기 쉽고 어떤 동작인지 알 수 있게 표현 

15. 함수 선언문 vs 함수 표현식  function sayHello(){} vs let sayHello = function(){}
			항상표현가능                   코드에 도달하면 생성 

16. this는 함수 전체를 의미함 --> 지역변수를 전역변수로 바꾸는 용어 지역변수(let으로 선언하기)<->전역변수 구분하면서 쓰기




17. form 태그에서 get vs post의 차이점:(method)
get은 가장 기본값이고 post는 데이터를 body에 붙여서 전달한다.