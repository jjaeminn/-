#include <stdio.h>

int mainlogg(void)
{
	/* int age = 15;
	if (age >= 20)
		printf("성인입니다.");					//***********조건문에는 ;이 들가면 안됨 --> 구분해주는 것 이니까
	else
		printf("미성년자입니다.");*/
	int age = 8;
	if (age >= 8 && age <= 13)    // && = and


		return 0;
}